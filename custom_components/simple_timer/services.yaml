# Describes the services for the Simple Timer integration
# All services accept either `entry_id` or `entity_id` to identify the target
# Simple Timer instance. Provide exactly one.

start_timer:
  name: Start Timer
  description: Starts a countdown timer for the device. The associated switch is turned on. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID; portable across systems).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    duration:
      name: Duration
      description: The duration of the timer in minutes.
      required: true
      selector:
        number:
          min: 1
          max: 1000
          unit_of_measurement: "minutes"
    reverse_mode:
      name: Reverse Mode
      description: If true, starts a delayed timer (device turns ON when timer finishes).
      required: false
      default: false
      selector:
        boolean:

add_timer:
  name: Add to Timer
  description: Extends a currently running timer by adding duration to it. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    duration:
      name: Duration
      description: The duration to add in minutes.
      required: true
      selector:
        number:
          min: 1
          max: 1000
          unit_of_measurement: "minutes"
    unit:
      name: Unit
      description: The unit of the duration (min, sec, etc).
      required: false
      default: minutes
      selector:
        select:
          options:
            - s
            - sec
            - seconds
            - m
            - min
            - minutes
            - h
            - hr
            - hours
            - d
            - day
            - days

schedule_timer:
  name: Schedule Timer
  description: Arm a future start. At the chosen time-of-day the switch turns on and runs a bounded timer for the given duration, then off. One-shot by default, or repeat on selected days. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    start_time:
      name: Start Time
      description: Time of day to start the timer (HH:MM). If already passed today, fires the next occurrence.
      required: true
      selector:
        time:
    duration:
      name: Duration
      description: How long to run once started.
      required: true
      selector:
        number:
          min: 1
          max: 1000
          unit_of_measurement: "minutes"
    unit:
      name: Unit
      description: The unit of the duration (min, sec, etc).
      required: false
      default: minutes
      selector:
        select:
          options:
            - s
            - sec
            - seconds
            - m
            - min
            - minutes
            - h
            - hr
            - hours
            - d
            - day
            - days
    repeat:
      name: Repeat
      description: If true, the schedule repeats on the selected days (or daily if none selected).
      required: false
      default: false
      selector:
        boolean:
    days:
      name: Days
      description: Days of week to repeat on (only used when Repeat is on). Empty means every day.
      required: false
      selector:
        select:
          multiple: true
          options:
            - mon
            - tue
            - wed
            - thu
            - fri
            - sat
            - sun

cancel_schedule:
  name: Cancel Schedule
  description: Cancel an armed scheduled start. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor

cancel_timer:
  name: Cancel Timer
  description: Cancels an active countdown timer. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    turn_off_entity:
      name: Turn Off Entity
      description: If true, turns off the associated switch entity when cancelling.
      required: false
      default: true
      selector:
        boolean:

update_switch_entity:
  name: Update Device Entity
  description: Tells the sensor which device entity to monitor for runtime calculation. Provide either entry_id or entity_id. A device whose "on" is a choice, such as a climate entity, is rejected unless that choice is already configured for this timer, and so is one that offers no way to turn it off.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    switch_entity_id:
      name: Device Entity
      description: The entity ID of the device to monitor (e.g., switch.my_timer, climate.living_room).
      required: true
      selector:
        entity:
          domain:
            - switch
            - input_boolean
            - light
            - fan
            - climate

force_name_sync:
  name: Force Name Sync
  description: Forces immediate synchronization of sensor names after entry rename. If neither entry_id nor entity_id is provided, syncs all Simple Timer sensors.
  fields:
    entry_id:
      name: Entry ID (Optional)
      description: The config entry ID to sync. If omitted, syncs all Simple Timer sensors.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity (Optional)
      description: The Simple Timer sensor entity to sync (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor

manual_power_toggle:
  name: Manual Power Toggle
  description: Manually toggles switch power and sends appropriate notifications. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor.
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    action:
      name: Action
      description: The action to perform (turn_on or turn_off).
      required: true
      selector:
        select:
          options:
            - turn_on
            - turn_off

reset_daily_usage:
  name: Reset Daily Usage
  description: Manually reset daily usage time to zero. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor

test_notification:
  name: Test Notification
  description: Test the notification functionality for a timer instance. Provide either entry_id or entity_id.
  fields:
    entry_id:
      name: Entry ID
      description: The config entry ID of the simple timer sensor
      required: false
      selector:
        config_entry:
          integration: simple_timer
    entity_id:
      name: Entity
      description: The Simple Timer sensor entity (alternative to Entry ID).
      required: false
      selector:
        entity:
          integration: simple_timer
          domain: sensor
    message:
      name: Message
      description: Test message to send
      required: false
      default: "Test notification"
      selector:
        text:

reload_resources:
  name: Reload Frontend Resources
  description: Manually reload and update the Simple Timer card frontend resources with the current version
  fields: {}
